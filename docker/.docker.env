# Container-only environment, merged in by docker-compose (env_file).
# The application's own configuration lives in the project-root .env, which is
# bind-mounted into the container — keep DB credentials etc. there.

CONTAINER_ROLE=app

# Set to "true" to auto-run the demo seeder on container start (first boot).
SEED_ON_START=false
